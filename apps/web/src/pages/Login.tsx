import { useState } from 'react';import{useNavigate}from'react-router-dom';import{useAuth}from'../store/auth';
export function Login(){const[error,setError]=useState('');const[form,setForm]=useState({email:'demo@calo.vn',password:'password123'});const login=useAuth(s=>s.login);const nav=useNavigate();async function submit(e:React.FormEvent){e.preventDefault();try{await login(form.email,form.password);nav('/')}catch{setError('Không đăng nhập được. Hãy seed database hoặc kiểm tra API.')}}return <main className="grid min-h-screen place-items-center bg-gradient-to-br from-green-50 to-white p-4"><form onSubmit={submit} className="card w-full max-w-md space-y-4"><h1 className="text-4xl font-black text-primary">Calo Việt</h1><p className="text-slate-500">Theo dõi calories, macro và cân nặng cho người Việt.</p><input className="input" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/><input className="input" type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/>{error&&<p className="text-danger">{error}</p>}<button className="btn w-full">Đăng nhập</button><p className="text-xs text-slate-400">Demo: demo@calo.vn / password123</p></form></main>}
