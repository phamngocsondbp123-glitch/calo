import{useEffect,useState}from'react';import{api}from'../lib/api';import{Line,LineChart,ResponsiveContainer,Tooltip,XAxis,YAxis}from'recharts';
export function Weight(){const[weights,setWeights]=useState<any[]>([]);const[weight,setWeight]=useState(60);async function load(){setWeights((await api.get('/weights')).data.weights.map((w:any)=>({...w,date:w.date.slice(0,10)})))}useEffect(()=>{void load()},[]);async function add(e:React.FormEvent){e.preventDefault();await api.post('/weights',{date:new Date().toISOString(),weight});await load()}return <section className="space-y-6"><h1 className="text-3xl font-black">Weight tracking</h1><form onSubmit={add} className="card flex gap-3"><input className="input" type="number" step="0.1" value={weight} onChange={e=>setWeight(Number(e.target.value))}/><button className="btn">Lưu cân nặng</button></form><div className="card h-96"><ResponsiveContainer><LineChart data={weights}><XAxis dataKey="date"/><YAxis/><Tooltip/><Line dataKey="weight" stroke="#16a34a" strokeWidth={3}/></LineChart></ResponsiveContainer></div></section>}
