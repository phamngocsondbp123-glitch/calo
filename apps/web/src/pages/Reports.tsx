import{useEffect,useState}from'react';import{api}from'../lib/api';import{Area,AreaChart,Bar,BarChart,Cell,Pie,PieChart,ResponsiveContainer,Tooltip,XAxis,YAxis}from'recharts';
export function Reports(){const[period,setPeriod]=useState('weekly');const[data,setData]=useState<any>();useEffect(()=>{void api.get(`/reports/${period}`).then(r=>setData(r.data))},[period]);const totals=(data?.nutrition??[]).reduce((a:any,d:any)=>({calories:a.calories+d.calories,protein:a.protein+d.protein,carbs:a.carbs+d.carbs,fat:a.fat+d.fat}),{calories:0,protein:0,carbs:0,fat:0});return <section className="space-y-6"><h1 className="text-3xl font-black">Reports & analytics</h1><select className="input max-w-xs" value={period} onChange={e=>setPeriod(e.target.value)}><option value="daily">Daily</option><option value="weekly">Weekly</option><option value="monthly">Monthly</option><option value="yearly">Yearly</option></select><div className="grid gap-4 lg:grid-cols-2"><div className="card h-80"><h2 className="font-bold">Calories over time</h2><ResponsiveContainer><AreaChart data={data?.nutrition??[]}><XAxis dataKey="date"/><YAxis/><Tooltip/><Area dataKey="calories" fill="#bbf7d0" stroke="#16a34a"/></AreaChart></ResponsiveContainer></div><div className="card h-80"><h2 className="font-bold">Macro distribution</h2><ResponsiveContainer><PieChart><Pie data={[{name:'Protein',value:totals.protein},{name:'Carbs',value:totals.carbs},{name:'Fat',value:totals.fat}]} dataKey="value" label>{['#16a34a','#60a5fa','#f97316'].map(c=><Cell fill={c} key={c}/>)}</Pie></PieChart></ResponsiveContainer></div><div className="card h-80"><h2 className="font-bold">Calories by meal</h2><ResponsiveContainer><BarChart data={Object.entries(data?.mealCalories??{}).map(([meal,calories])=>({meal,calories}))}><XAxis dataKey="meal"/><YAxis/><Tooltip/><Bar dataKey="calories" fill="#16a34a"/></BarChart></ResponsiveContainer></div><div className="card"><h2 className="font-bold">Top 10 foods</h2>{data?.topFoods?.map((f:any)=><p className="mt-2 flex justify-between" key={f.food}><span>{f.food}</span><b>{f.servings} lần</b></p>)}</div></div></section>}
